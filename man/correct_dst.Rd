% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/correct_dst.R
\name{correct_dst}
\alias{correct_dst}
\title{Correct TRAFx ShuttleFiles for Daylight Saving Time}
\usage{
correct_dst(
  path,
  direction = c("begin", "end"),
  year,
  skip_no_change = TRUE,
  output_dir = NULL
)
}
\arguments{
\item{path}{Character. Path to a single ShuttleFile (.txt) or a folder of files.}

\item{direction}{Character. Either "begin" to advance 1 hour (spring forward)
or "end" to subtract 1 hour (fall back).}

\item{year}{Numeric. Full 4-digit year of the DST change (used to auto-calculate date).}

\item{skip_no_change}{Logical. If TRUE, files that do not span the DST change are skipped. Defaults to TRUE.}

\item{output_dir}{Character. Optional. Directory to save corrected files. Defaults to a "dst_corrected" folder inside the input folder.}
}
\value{
NULL. Writes corrected ShuttleFiles to the specified output folder.
}
\description{
Adjusts timestamps in TRAFx ShuttleFiles for DST changes (spring or fall).
Automatically computes the DST change date for the specified year and optionally
skips files that do not span the DST change. Skips files that have already been processed
that contain "_DST_Corrected" in the filename.
}
\examples{
\dontrun{
# Correct all files in a folder for spring DST
correct_dst("data/trafx_downloads/", direction = "begin", year = 2024)

# Correct a single file for fall DST
correct_dst("data/trafx_downloads/ShuttleFile_001.txt", direction = "end", year = 2024)
}
}
