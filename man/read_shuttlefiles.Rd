% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read_shuttlefile_folder.R
\name{read_shuttlefiles}
\alias{read_shuttlefiles}
\title{Read and process TRAFx ShuttleFiles}
\usage{
read_shuttlefiles(folder, return = c("both", "counts", "header"))
}
\arguments{
\item{folder}{Character. Path to folder containing ShuttleFiles (.txt)}

\item{return}{Character. Which data to return: "both" (default), "counts", or "header"}

\item{tz}{Character. Time zone for POSIXct conversion; defaults to system TZ}

\item{split_output}{Logical. If TRUE and return = "both", creates 'counts' and 'header' directly in the user's environment}
}
\value{
A list with elements `counts` and `header` (if `return = "both"`), or a single data frame
}
\description{
Reads all ShuttleFiles in a folder, parses hourly counts and download metadata,
and returns a list with counts and header information. Optionally, it can
create 'counts' and 'header' objects directly in the user environment.
}
\examples{
\dontrun{
# Return list
result <- read_shuttlefiles("data/trafx_2025")
counts_df <- result$counts
header_df <- result$header

# Split output directly to environment
read_shuttlefiles("data/trafx_2025", split_output = TRUE)
head(counts)
head(header)
}
}
